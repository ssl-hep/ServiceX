---
{{ if .Values.didFinder.xrootd.enabled}}
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ .Release.Name }}-did-finder-xrootd
spec:
  replicas: 1
  selector:
    matchLabels:
      app: {{ .Release.Name }}-did-finder-xrootd
  template:
    metadata:
      labels:
        app: {{ .Release.Name }}-did-finder-xrootd
      {{- with .Values.didFinder.xrootd.podAnnotations }}
      annotations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
    spec:
      {{- with .Values.didFinder.xrootd.nodeSelector }}
      nodeSelector:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.didFinder.xrootd.affinity }}
      affinity:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.didFinder.xrootd.tolerations }}
      tolerations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      initContainers:
        - name: check-url
          image: {{ .Values.app.checksImage }}
          env:
          - name: URL
            value: "http://{{ .Release.Name }}-servicex-app:8000"
      containers:
      - name: {{ .Release.Name }}-did-finder-xrootd
        image: {{ .Values.didFinder.xrootd.image }}:{{ .Values.didFinder.xrootd.tag }}
        imagePullPolicy: {{ .Values.didFinder.CERNOpenData.pullPolicy }}
        {{- if .Values.didFinder.xrootd.resources }}
        resources:
{{ toYaml .Values.didFinder.xrootd.resources | indent 10 }}
        {{- end }}
        env:
          - name: INSTANCE_NAME
            value: {{ .Release.Name }}
        {{- if .Values.secrets }}
          - name: RMQ_PASS
            valueFrom:
              secretKeyRef:
                name: {{ .Values.secrets }}
                key: rabbitmq-password
          - name: BROKER_URL
            value: amqp://user:$(RMQ_PASS)@{{ .Release.Name }}-rabbitmq:5672/?heartbeat=9000
        {{- else }}
          - name: BROKER_URL
            value: amqp://user:{{ .Values.rabbitmq.auth.password }}@{{ .Release.Name }}-rabbitmq:5672/%2F
        {{- end }}
        {{- if .Values.logging.logstash.enabled }}
          - name: LOGSTASH_HOST
            value: "{{ .Values.logging.logstash.host }}"
          - name: LOGSTASH_PORT
            value: "{{ .Values.logging.logstash.port }}"
        {{- end }}
        {{ if not .Values.noCerts}}
        volumeMounts:
          - name: x509-secret
            mountPath: /etc/grid-security-ro
            readOnly: true
        {{- end }}
      {{ if not .Values.noCerts}}
      volumes:
        - name: x509-secret
          secret:
            defaultMode: 292
            secretName: x509-proxy
      {{- end }}
{{ end }}
