apiVersion: apps/v1
kind: Deployment
metadata:
  name: servicex-lite
  namespace: servicex-lite
  labels:
    k8s-app: servicex-lite
spec:
  replicas: 1
  selector:
    matchLabels:
      k8s-app: servicex-lite
  template:
    metadata:
      labels:
        k8s-app: servicex-lite
    spec:
      containers:
        - name: servicex-lite
          image: sslhep/servicex-lite:latest
          imagePullPolicy: Always
          command: ["sleep"]
          args: ["infinity"]
          volumeMounts:
            - name: kubeconfig
              mountPath: /home/sxlite/.kube/
          resources:
            requests:
              memory: "256Mi"
              cpu: "250m"
            limits:
              memory: "512Mi"
              cpu: "500m"
      volumes:
        - name: kubeconfig
          secret:
            secretName: kubeconfig-secret
