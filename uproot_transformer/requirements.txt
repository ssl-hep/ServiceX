uproot==4.1.9
awkward==1.7.0
pyarrow==3.0.0
# servicex-transformer==1.0.5
servicex-transformer==1.0.7rc01
vector==0.8.4
minio==7.1.12
pika==1.1.0
psutil==5.8.0
python-logstash == 0.4.8

# There is no way the small amount of code written in this repo needs all this - are these
# dependencies and they should just included there?
